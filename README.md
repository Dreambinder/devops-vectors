# Vectors Project

This is a simple Python script that plots vectors on a 2D plane using the `matplotlib` library.

## Prerequisites
1. Make sure you have Python installed
2.Install the library matplotlib `pip install matplotlib`
3. Run the file `python vectors.py`
